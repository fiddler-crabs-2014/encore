<div id="follow_form">
  <% if current_user.following?(user) %>
    <%= render partial: "unfollow", locals: { followed_user: user } %>
  <% else %>
    <%= render partial: "follow", locals: { other_user: user } %>
  <% end %>
</div>
